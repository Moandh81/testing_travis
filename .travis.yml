
language: python

python:
  - "3.7"
  - "3.6"

services:
  - docker

before-install:
  - docker pull python
  - docker run --name mypython python
  - docker exec -it mypython unittest tests.py


script: python tests.py